# Example htpasswd file - DO NOT USE IN PRODUCTION
# Generate a real htpasswd file using:
#   podman compose run --rm web htpasswd -nbB admin yourpassword > lib/htpasswd
#
# The format is: username:encrypted_password
# Example with bcrypt encryption:
admin:$2y$05$examplehashdonotuse
